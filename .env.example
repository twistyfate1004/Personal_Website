# Required for /admin login in local development and production.
ADMIN_PASSWORD=change-this-password

# Optional but recommended in production. Use a long random string.
ADMIN_SECRET=change-this-to-a-long-random-secret

# Optional. Defaults to ./storage/site-content.db
DATABASE_PATH=./storage/site-content.db
